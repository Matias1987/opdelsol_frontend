/**
 * VALIDAR CAMPOS VENTA!!!!! TO DO 
 */

const validar_items_venta = (tipo_venta, campos) => {
/**
 * Si un elemento esta visible hay que verificar que 
 * el el resto de los campos tenga valor!
 */
}

module.exports = {validar_items_venta}